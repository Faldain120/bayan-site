# Normalize line endings to LF in the repo (prevents cross-machine autocrlf drift)
* text=auto eol=lf

# Binary assets — never touch
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
